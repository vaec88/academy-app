# Steps to launch the project

1. Launch MongoDB
2. Create the `academy` database
3. Delete the `student.json`, `course.json` and `enrollment.json` files in `src/main/resources/test-data/` path
4. Use `generate-test-data.md` workflow for create data for new Student, Course, Enrollment documents
5. Launch the application